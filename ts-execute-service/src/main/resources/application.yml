server:
  port: 12386

spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDRS:nacos}
  application:
    name: ts-execute-service
swagger:
  controllerPackage: execute.controller