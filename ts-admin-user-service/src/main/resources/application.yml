server:
  port: 16115

spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDRS:nacos}
  application:
    name: ts-admin-user-service

swagger:
  controllerPackage: adminuser.controller