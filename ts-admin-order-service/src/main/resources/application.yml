server:
  port: 16112

spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_ADDRS:nacos}
  application:
    name: ts-admin-order-service


swagger:
  controllerPackage: adminorder.controller


